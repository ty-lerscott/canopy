name: Deploying Canopy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
#      - name: Setup Node.js
#        uses: actions/setup-node@v4
#        with:
#          node-version: "20"

#      - name: Install dependencies
#        run: npm ci
#
#      - name: Build application
#        run: npm run build

#      - name: Set up SSH
#        uses: webfactory/ssh-agent@v0.5.1
#        with:
#          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}
      - name: Tarballer
        run: |
          tar -czvf canopy.tar.gz ./ --exclude=".git" --exclude=".next" --exclude="node_modules"

      - name: Publish
        uses: nogsantos/scp-deploy@master
        with:
          src: canopy.tar.gz
          host: ${{ secrets.DROPLET_IP }}
          remote: ${{ secrets.DROPLET_TARGET_DIR }}
          port: 22
          user: ${{ secrets.DROPLET_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
#      - name: Deploy to DigitalOcean
#        run: |
#          scp -r ./ ${{ secrets.DROPLET_USER }}@${{ secrets.DROPLET_IP }}:${{ secrets.DROPLET_TARGET_DIR }}
#          ssh ${{ secrets.DROPLET_USER }}@${{ secrets.DROPLET_IP }} << EOF
#            cd ${{ secrets.DROPLET_TARGET_DIR }}
#            npm ci --production
#            npm run build
#            pm2 restart ecosystem.config.js
#          EOF